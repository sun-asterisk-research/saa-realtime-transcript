{{- if .Values.web.ingress.enabled -}}
{{- include "tplchart.ingress" (dict
  "context" .
  "Args" (dict
    "nameTemplate" "%s"
    "component" "web"
    "service" (dict
      "nameTemplate" "%s"
      "port" "http"
    )
  )
  "Scope" "web"
) -}}
{{- end -}}
