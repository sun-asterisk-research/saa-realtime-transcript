{{- if .Values.realtime.ingress.enabled -}}
{{- include "tplchart.ingress" (dict
  "context" .
  "Args" (dict
    "nameTemplate" "%s-realtime"
    "component" "realtime"
    "service" (dict
      "nameTemplate" "%s-realtime"
      "port" "ws"
    )
  )
  "Scope" "realtime"
) -}}
{{- end -}}
