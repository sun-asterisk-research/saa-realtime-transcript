name: Deploy Supabase Database

on:
  push:
    branches:
      - realtime_translate
  workflow_dispatch:

env:
  SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
  SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}

jobs:
  deploy-database:
    name: Deploy Database & Seed Data
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link Supabase project
        run: supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}

      - name: Push migrations and seeds
        run: supabase db push --include-all

      - name: Deployment summary
        if: success()
        run: |
          echo "✅ Database deployment successful!"
          echo "✅ Migrations applied"
          echo "✅ Seed data loaded (common/*.sql)"
          echo "📦 Context sets: Technology, Business, Medical, Sun Asterisk"
